{
  "name": "entangle",
  "displayName": "entangle",
  "description": "Hand a Claude Code session to a teammate over an encrypted code, or share it to a file — without leaving VS Code.",
  "version": "0.1.0",
  "publisher": "gowtham-sai-yadav",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "claude",
    "claude code",
    "session",
    "share",
    "ai"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/gowtham-sai-yadav/entangle"
  },
  "homepage": "https://gowthamsai.in/teleport/",
  "bugs": {
    "url": "https://github.com/gowtham-sai-yadav/entangle/issues"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "entangle",
          "title": "Teleport",
          "icon": "media/teleport.svg"
        }
      ]
    },
    "views": {
      "entangle": [
        {
          "id": "entangle.sessions",
          "name": "Sessions",
          "icon": "media/teleport.svg"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "entangle.sessions",
        "contents": "No Claude Code sessions to show.\n[Refresh](command:entangle.refresh)\n[Receive a session](command:entangle.receive)\n\nThe entangle CLI does the work — make sure it is installed.\n[Install entangle](https://gowthamsai.in/teleport/#install)"
      }
    ],
    "commands": [
      {
        "command": "entangle.send",
        "title": "Send a session by code",
        "category": "Teleport"
      },
      {
        "command": "entangle.receive",
        "title": "Receive a session",
        "category": "Teleport",
        "icon": "$(inbox)"
      },
      {
        "command": "entangle.share",
        "title": "Share a session to a file",
        "category": "Teleport"
      },
      {
        "command": "entangle.sessions",
        "title": "Browse sessions",
        "category": "Teleport"
      },
      {
        "command": "entangle.menu",
        "title": "Open menu",
        "category": "Teleport"
      },
      {
        "command": "entangle.refresh",
        "title": "Refresh sessions",
        "category": "Teleport",
        "icon": "$(refresh)"
      },
      {
        "command": "entangle.sendItem",
        "title": "Send by code",
        "category": "Teleport",
        "icon": "$(radio-tower)"
      },
      {
        "command": "entangle.shareItem",
        "title": "Share to a file",
        "category": "Teleport",
        "icon": "$(file)"
      },
      {
        "command": "entangle.copyItemId",
        "title": "Copy session id",
        "category": "Teleport"
      }
    ],
    "menus": {
      "commandPalette": [
        { "command": "entangle.menu", "when": "false" },
        { "command": "entangle.sendItem", "when": "false" },
        { "command": "entangle.shareItem", "when": "false" },
        { "command": "entangle.copyItemId", "when": "false" }
      ],
      "view/title": [
        { "command": "entangle.receive", "when": "view == entangle.sessions", "group": "navigation@1" },
        { "command": "entangle.refresh", "when": "view == entangle.sessions", "group": "navigation@2" }
      ],
      "view/item/context": [
        { "command": "entangle.sendItem", "when": "view == entangle.sessions && viewItem == session", "group": "inline@1" },
        { "command": "entangle.shareItem", "when": "view == entangle.sessions && viewItem == session", "group": "inline@2" },
        { "command": "entangle.sendItem", "when": "view == entangle.sessions && viewItem == session", "group": "1_actions@1" },
        { "command": "entangle.shareItem", "when": "view == entangle.sessions && viewItem == session", "group": "1_actions@2" },
        { "command": "entangle.copyItemId", "when": "view == entangle.sessions && viewItem == session", "group": "1_actions@3" }
      ]
    },
    "configuration": {
      "title": "entangle",
      "properties": {
        "entangle.path": {
          "type": "string",
          "default": "entangle",
          "description": "Path to the entangle binary. Leave as the default if it is on your PATH."
        },
        "entangle.configDir": {
          "type": "string",
          "default": "",
          "description": "Override the Claude config dir (passed to the CLI as --config-dir). Leave empty for the default (~/.claude)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "npm run compile && node ./test/verify.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0"
  }
}
